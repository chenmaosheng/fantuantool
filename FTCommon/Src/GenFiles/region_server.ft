filter (RegionServer, SERVER_FILTER_REGION)
{
	ServerTimeNtf(uint32 iServerTime);
	InitialAvatarDataNtf(uint64 iAvatarId, string strAvatarName);
	
	RegionAvatarEnterNtf(uint64 iAvatarId, string strAvatarName);
	RegionAvatarLeaveNtf(uint64 iAvatarId);
	RegionChatNtf(uint64 iAvatarId, string strMessage);
}